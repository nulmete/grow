import styled from "styled-components";
import { Card } from "../../common/components";

export const GridItemCard = styled(Card)`
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
`;
